{
  "name": "ecproject",
  "version": "1.0.0",
  "private": true,
  "description": "ECPROJECT monorepo — Next.js client + Express API/admin. Deployed as one Railway service (see RAILWAY.md).",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm ci --prefix server && npm ci --prefix client --include=dev && npm --prefix client run build",
    "start": "node start.js"
  }
}
